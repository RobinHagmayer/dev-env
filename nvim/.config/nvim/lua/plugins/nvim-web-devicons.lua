-- NOTE: blink.cmp provides its own kind icons, so this may only be needed for
-- future file explorers or statusline plugins.
-- TODO: audit whether this plugin is still needed
vim.pack.add({ "https://github.com/nvim-tree/nvim-web-devicons" })
